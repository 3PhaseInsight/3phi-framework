# Optional overrides for local DB schema provisioning.
# Copy to dev.env (gitignored) and edit as needed.
# Git ref (branch or tag) in 3PhaseInsight/data-platform to pull sqitch migrations from.
MIGRATIONS_REF=main
